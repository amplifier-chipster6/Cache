[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cache-tool-wrapper"
version = "0.1.0"
description = "Cache tool wrapper"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["cache_tool_wrapper*"]
